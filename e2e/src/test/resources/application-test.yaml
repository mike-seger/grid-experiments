spring:
  datasource:
    url: jdbc:h2:mem:adminux-test;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: sa
    driverClassName: org.h2.Driver
  jpa.hibernate.ddl-auto: create-drop

logging.level:
  root: warn
  liquibase: warn
  com.zaxxer.hikari: warn
  org.springframework.test: info
